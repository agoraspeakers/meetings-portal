<nav class="navbar navbar-expand-lg navbar-light bg-light" id="top-navigation">
  <div class="container-fluid">
    <%= link_to t('.title'), root_path, class: 'navbar-brand' %>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarResponsive">
      <!-- Left navigation -->
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <%= link_to root_path, class: 'nav-link' do %>
            <%= t('.home') %>
            <span class="sr-only">(current)</span>
          <% end %>
        </li>
        <li class="nav-item">
          <%= link_to t('.users'), users_path, class: 'nav-link' %>
        </li>
      </ul>
      <!-- Right navigation -->
      <ul class="navbar-nav ml-auto">
        <!-- Nav Item - User authentication -->
        <% if current_user %>
          <li class="nav-item">
            <%= link_to t('.logout'), destroy_user_session_path, method: :delete, class: 'nav-link' %>
          </li>
        <% else %>
          <li class="nav-item">
            <%= link_to t('.sign_in_with_facebook'), user_facebook_omniauth_authorize_path, method: :post, class: 'nav-link' %>
          </li>
          <li class="nav-item">
            <%= link_to t('.login'), new_user_session_path, class: 'nav-link' %>
          </li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
